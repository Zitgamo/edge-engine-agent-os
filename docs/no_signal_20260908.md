# Kiểm tra không có tín hiệu — 08/09/2026

## Kế hoạch và tiêu chí hoàn thành

1. Đọc trạng thái cloud và local, đối chiếu log đúng lần chạy. Không dùng log local để giải thích một phiên cloud khác.
2. Phân biệt ngày dữ liệu, ngày thực thi và ngày tín hiệu. Giữ cảnh báo NO TRADE, khôi phục trạng thái legacy thiếu run_key và cảnh báo độ mới cho run cũ.
3. Sửa fixture dashboard; kiểm thử dữ liệu cũ, cuối tuần, khoảng đợi cập nhật cuối ngày, legacy và toàn bộ bộ test offline.
4. Ghi kết quả và xác định có cần chạy lại pipeline. Không thay đổi ngưỡng lọc hoặc ép phát tín hiệu.

## Bằng chứng đã xác minh

- Cloud: phiên 2026-09-07, thực thi 2026-09-07 22:08:06 giờ VN, `no_trade`.
- Tín hiệu cloud gần nhất: 2026-09-03 (TCB, PNJ, VPB).
- GitHub Actions run 34136400479: completed/success. Model vượt quality gate; thu thập được 97/126 mã.
- Log đúng run: `market breadth below threshold`, `market_breadth_20d=0.32989690721649484`, `min_market_breadth_20d=0.5`. Có 32/97 mã tăng trong 20 phiên; bộ lọc chặn toàn bộ 97 ứng viên.
- Local chỉ có run gần nhất 2026-09-04 và tín hiệu 2026-08-05; không đại diện cho cloud hiện tại.
- Cloud chưa trả về diagnostics. Không ghi đè báo cáo lịch sử bằng kết quả tái tính toán local.

Nguồn: https://github.com/Zitgamo/edge-engine-agent-os/actions/runs/34136400479

## Thay đổi

- Trạng thái run và độ mới được hiển thị riêng. SUCCESS cũ không còn có màu xanh.
- Kiểm tra độ mới dùng ngày làm việc gần nhất, đợi đến 19:00 giờ VN cho các lượt cập nhật cuối ngày, bỏ qua cuối tuần. Đây là cảnh báo cần kiểm tra, không phải kết luận lỗi: chưa có lịch nghỉ lễ của sở giao dịch.
- Legacy thiếu run_key dùng ngày thực thi theo giờ VN để giữ cảnh báo, nhưng không suy diễn ngày dữ liệu.
- Bổ sung `actual_outperform=None` đúng hợp đồng get_signals cho fixture pending.

## Quyết định vận hành

Không cần ép chạy lại phiên 07/09: pipeline đã hoàn tất và chặn theo quy tắc hiện hành. Không hạ ngưỡng 50%. Bản sửa và migration diagnostics cần được triển khai để cloud hiển thị báo cáo các lần chạy mới; thay đổi local không tự cập nhật bản đang chạy trên cloud.

## Kiểm thử

- Bộ test offline trong thư mục tạm, chặn kết nối ngoài: **174 passed** (32,72 giây), Python 3.11.15. CI dùng Python 3.12; chưa chạy bản sửa trên CI.
- Bằng chứng: `docs/audit_runs/20260908T031448254334Z/test-run.json`, `test-results.xml`, `coverage.json`.
- Sau chỉnh sửa bổ sung để phiên chạy lại cũ không làm tín hiệu mới bị báo chậm: **13/13 test diagnostics passed**.
- `ruff check src tests`: passed. `git diff --check`: passed.
- Đã hoàn tất kiểm tra nguyên nhân và sửa local. Chưa push/deploy hoặc áp dụng migration lên cloud.
