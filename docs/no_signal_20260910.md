# Kiểm tra không có tín hiệu — 10/09/2026

## Kế hoạch và tiêu chí hoàn thành

1. Đọc trực tiếp cloud và log Actions; phân biệt ngày phiên với ngày chạy lại.
2. Đối chiếu phiên 07–09/09, ghi nguyên nhân đúng từng lần chạy; không suy diễn từ trạng thái success của workflow.
3. Sửa lỗi collection failure ghi đè publication thành công: giữ status, metrics, thời gian và signals; lưu lần thử lỗi trong diagnostics và hiển thị riêng trên dashboard.
4. Kiểm thử trường hợp chưa có publication, đã có publication, và lần chạy thành công sau đó; chạy toàn bộ test trong thư mục tạm với mạng bị chặn.
5. Ghi giới hạn triển khai. Không đổi ngưỡng, không chạy lại model để ép có tín hiệu, không ghi lại dữ liệu cloud lịch sử.

## Kết quả kiểm tra trực tiếp

Cloud đọc ngày 10/09/2026: tín hiệu gần nhất 03/09, gồm TCB, PNJ, VPB.

| Phiên | Trạng thái cloud mới nhất | Bằng chứng |
|---|---|---|
| 07/09 | no_trade | Log kiểm tra trước: breadth 32/97 = 32,99%, dưới 50%. |
| 08/09 | quality_failed | Chạy lại 09/09 lúc 13:03 giờ VN: execution top-3 excess return −0,30%, dưới 0%. |
| 09/09 | challenger_rejected | Chạy lúc 20:48 giờ VN: mô hình mới giảm excess return 0,46 điểm %, spread 0,65 điểm %, quá dung sai 0,20 điểm %. |

Nguồn:
- https://github.com/Zitgamo/edge-engine-agent-os/actions/runs/34136400479
- https://github.com/Zitgamo/edge-engine-agent-os/actions/runs/34317212687
- https://github.com/Zitgamo/edge-engine-agent-os/actions/runs/34359112491

Lần chạy tối 08/09 từng ghi challenger_rejected (spread giảm 0,29 điểm %), nhưng lần chạy trưa 09/09 đã thay bản ghi cùng phiên bằng quality_failed. Không nhầm hai lần chạy này. Cả hai lượt được kiểm tra cho 08 và 09/09 thu thập 97/126 mã; có lỗi nguồn ở một số mã nhưng không dừng vì thiếu số lượng tối thiểu.

Kết luận: pipeline có chạy; ngừng phát tín hiệu do các cổng kiểm soát. Không đủ cơ sở khẳng định chiến lược tốt hoặc sẽ sớm phát lại. Ngày 10/09 chưa có phiên đóng cửa tại thời điểm kiểm tra.

## Bản sửa

- Khi data_failed gặp publication success cùng run_key, giữ nguyên kết quả đã phát, lưu last_failed_attempt trong diagnostics. Một lần thành công mới xóa cảnh báo cũ bằng báo cáo mới.
- Dashboard cảnh báo riêng lần chạy lại lỗi, không tuyên bố phiên đã phát tín hiệu là DATA FAILED.
- Schema diagnostics hiện có được tái sử dụng. Cloud trả về chưa có trường diagnostics nên cần migration có sẵn trước khi có thể hiển thị chi tiết mới.

## Triển khai

Bản phát hành được chuẩn bị trên nhánh `deploy/data-health-fixes-20260909`, dựa trên `origin/master` commit `58c08dd`. Chỉ ba tệp mã/test và tài liệu đã review được đưa vào thay đổi; dữ liệu raw/research và audit bulky không được commit. Migration Supabase đã chạy thành công: cột `pipeline_runs.diagnostics` là `jsonb`, PostgREST đã reload schema và Data API đọc được cột. PR #2 đã merge vào `master` (`c006a7c`), CI xanh, và lượt `daily.yml` sau deploy `34498156603` hoàn tất thành công. Cloud run ngày 10/09 ghi `no_trade` cùng diagnostics; dashboard live hiển thị đúng cùng trạng thái và nguyên nhân breadth gate.

## Kiểm chứng

- Toàn bộ bộ test offline: **179 passed**, 35,57 giây, Python 3.11.15; không dùng dữ liệu runtime hoặc kết nối ngoài.
- Ruff cho ba file sửa: passed. `git diff --check`: passed.
- Bằng chứng: `docs/audit_runs/20260910T035839417764Z/test-run.json` và `test-results.xml`.
- Lần kiểm tra đầu phát hiện lỗi cú pháp trong test mới; đã sửa và chạy lại toàn bộ thành công.
